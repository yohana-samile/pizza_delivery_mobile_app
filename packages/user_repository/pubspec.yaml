name: user_repository
description: repo that handle users
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

#  equatable: ^2.0.5
  cloud_firestore: ^4.9.2
  firebase_auth: ^4.16.0
#  firebase_storage: ^11.6.0
  rxdart: ^0.27.7
#  path: ^1.8.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true